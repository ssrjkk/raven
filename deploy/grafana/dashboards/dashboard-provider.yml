apiVersion: 1

providers:
  - name: "Raven Default"
    orgId: 1
    folder: "Raven Services"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
