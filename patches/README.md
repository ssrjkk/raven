# Dependency Patches

This directory contains patches for third-party dependencies.

## Format

Patches follow the naming convention: `<package-name>@<version>.patch`

## Current Patches

None yet. Patches are applied via `patch -p1 < patches/<name>.patch` after `pip install`.
